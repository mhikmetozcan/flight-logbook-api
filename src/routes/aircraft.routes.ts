import { Router } from "express";

const router = Router();

// TODO: build this yourself, following the pilot.routes.ts pattern:
//   POST   /api/aircraft         register an aircraft (admin only)
//   GET    /api/aircraft         list aircraft
//   GET    /api/aircraft/:id     get one aircraft + its flight history
//   PATCH  /api/aircraft/:id     update aircraft details

export default router;
