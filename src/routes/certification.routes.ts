import { Router } from "express";

const router = Router();

// TODO: build this yourself, following the pilot.routes.ts pattern:
//   POST   /api/certifications          add a certification for a pilot
//   GET    /api/certifications?pilotId= list a pilot's certifications
//   GET    /api/certifications/expiring list certifications expiring soon
//                                       (good place for a real query with
//                                       Prisma's date filtering)

export default router;
